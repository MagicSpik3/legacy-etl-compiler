---
title: "Legacy ETL Compiler"
author: "Mark London"
date: "2026-03-05"

format:
  revealjs:
    theme: simple
    slide-number: true
    footer: "WAS RADAR"
    logo: "WAS_RADAR.png"
    chalkboard: false

css: styles.css

execute:
  echo: false
  warning: false
---

## Overview

- Purpose: migrate SPSS/PSPP logic into R pipelines
- Verification & validation cycle

---

## Architecture

```{dot}
digraph {
  rankdir=LR;
  bgcolor="transparent";
  fontname="Helvetica";
  fontsize=22;

  node [
    shape=box
    style="rounded,filled"
    fontname="Helvetica"
    fontsize=20
    penwidth=2
  ];

  edge [
    color="#424242"
    penwidth=3
  ];

  subgraph cluster_parser {
    label="Parser & Topology";
    fontsize=24;
    style=filled;
    fillcolor="#E3F2FD";
    color="#1565C0";
    penwidth=2;

    A [label="SPSS / PSPP Script", fillcolor="#BBDEFB", color="#0D47A1"];
    B [label="GraphBuilder", fillcolor="#90CAF9", color="#0D47A1"];
    C [label="Raw Topology YAML", fillcolor="#64B5F6", color="#0D47A1"];

    A -> B -> C;
  }

  subgraph cluster_optimizer {
    label="Optimizer";
    fontsize=24;
    style=filled;
    fillcolor="#E8F5E9";
    color="#2E7D32";
    penwidth=2;

    D [label="Optimized Topology", fillcolor="#A5D6A7", color="#1B5E20"];

    C -> D;
  }

  subgraph cluster_codegen {
    label="Code Generation";
    fontsize=24;
    style=filled;
    fillcolor="#FFF3E0";
    color="#E65100";
    penwidth=2;

    E [label="R Generator", fillcolor="#FFCC80", color="#E65100"];
    F [label="Pipeline.R", fillcolor="#FFB74D", color="#E65100"];

    D -> E -> F;
  }

  subgraph cluster_verif {
    label="Verification";
    fontsize=24;
    style=filled;
    fillcolor="#F3E5F5";
    color="#6A1B9A";
    penwidth=2;

    G [label="PSPP Output", fillcolor="#CE93D8", color="#4A148C"];
    H [label="Rscript Output", fillcolor="#BA68C8", color="#4A148C"];

    A -> G [style=dashed, color="#4A148C"];
    F -> H [style=dashed, color="#4A148C"];
  }
}

```

---

## What is the legacy-etl-compiler system?

* Takes an existing SPSS/PSPP pipeline and converts it into another language (e.g., R).
* Uses an intermediate representation to make the compiler deterministic, testable, and optimizable.
* The key intermediate representation is a **State Machine** built from the original script.

---

## Why not translate directly to R?

* **The Syntax Trap:** Direct translation maps SPSS syntax directly to R syntax. This fails because the two languages have fundamentally different execution models.
* **Hidden Side Effects:** SPSS is highly procedural and relies on a mutable "active dataset." R leans towards functional dataframe manipulation.
* **Brittle Output:** Without an intermediate logical layer, the resulting R code is often unreadable, unoptimized, and extremely difficult to test for true equivalence.

---

## Why via a State Machine?

* A State Machine models the true underlying *territory* of the data lifecycle, independent of the *map* (the syntax) of either language.
* It makes dependencies explicitly clear and eliminates hidden side effects.
* It safely allows the compiler to reorder, optimize, and mathematically verify transformations while ensuring behavioral equivalence.

---

## What is a State Machine?

* A mathematical model of a program where each operation explicitly transitions the system to a new, immutable state.
* Instead of mutating variables, we create new versions at each step (e.g., `X1`, `X2`, `X3`).
* Each step is deterministic, making complex, messy legacy pipelines significantly easier to reason about.

---

## Order Matters (Example)

Consider the following sequential logic:

```text
X <- 1
Y <- X * 3
X <- X + 1
Z <- X * 7
output sum X, Y, Z

```

If run in this order, the values are:

* X becomes 1
* Y becomes 3
* X becomes 2
* Z becomes 14
* sum = 19

If the operations are reordered (e.g., computing Z before updating Y), the result changes.

---

## State Machine View (Same Logic)

Each step creates a new state (immutable datasets):

* `X1` is given the value 1.
* `Y` is computed from `X1` (3).
* `X2` is updated from `X1` (2).
* `Z` is computed from `X2` (14).
* Output is the sum of `X2`, `Y`, and `Z` (19).

```{dot}
digraph {
  rankdir=LR;
  bgcolor="transparent";
  
  node [
    shape=box, 
    style="rounded,filled", 
    fontname="Helvetica", 
    fontsize=20,
    penwidth=2
  ];
  
  edge [color="#424242", penwidth=2];

  S0 [label="State₀\nX=1", fillcolor="#FFCDD2", color="#C62828"];
  S1 [label="State₁\nY=3", fillcolor="#FFF9C4", color="#F57F17"];
  S2 [label="State₂\nX=2", fillcolor="#C8E6C9", color="#2E7D32"];
  S3 [label="State₃\nZ=14", fillcolor="#B3E5FC", color="#0277BD"];
  S4 [label="State₄\nOutput=19", fillcolor="#E1BEE7", color="#6A1B9A"];

  S0 -> S1 -> S2 -> S3 -> S4;
}

```

---

## State Machine (Compiler-Level)

* Operations become nodes in a directed acyclic graph.
* States are the intermediate datasets produced by each node.
* The compiler can then optimize, reorder, and validate these transformations.

---

## Environment Considerations

* Linux (Rscript + PSPP easily installed)
* Windows: R via VS Code, PSPP absent -> configurable commands or skip

---

## Usage

```bash
python src/compiler.py --manifest demo/input/logic.sps \
  --pspp-cmd "echo PSPP skipped" \
  --rscript-cmd Rscript

```

---

## Questions?

*Detailed notes available in `docs/system_explanation.qmd`.*

```