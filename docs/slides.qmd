---
title: "Legacy ETL Compiler"
author: "Mark London"
date: "2026-03-05"

format:
  revealjs:
    theme: simple
    slide-number: true
    footer: "WAS RADAR"
    logo: "WAS_RADAR.png"
    chalkboard: false

css: styles.css

execute:
  echo: false
  warning: false
---

## Overview

- Purpose: migrate SPSS/PSPP logic into R pipelines
- Verification & validation cycle

---

## Architecture

```{dot}
digraph {

  rankdir=LR;
  bgcolor="transparent";
  fontname="Helvetica";
  fontsize=22;

  node [
    shape=box
    style="rounded,filled"
    fontname="Helvetica"
    fontsize=20
    fillcolor="#F8F9FA"
    color="#1F4E79"
    penwidth=2
  ];

  edge [
    color="#1F4E79"
    penwidth=3
  ];

  subgraph cluster_parser {
    label="Parser & Topology";
    fontsize=24;
    style=filled;
    fillcolor="#E8F1FA";
    color="#1F4E79";
    penwidth=2;

    A [label="SPSS / PSPP Script"];
    B [label="GraphBuilder"];
    C [label="Raw Topology YAML"];

    A -> B -> C;
  }

  subgraph cluster_optimizer {
    label="Optimizer";
    fontsize=24;
    style=filled;
    fillcolor="#EAF7EA";
    color="#2E7D32";
    penwidth=2;

    D [label="Optimized Topology YAML"];

    C -> D;
  }

  subgraph cluster_codegen {
    label="Code Generation";
    fontsize=24;
    style=filled;
    fillcolor="#FFF3E0";
    color="#E65100";
    penwidth=2;

    E [label="R Generator"];
    F [label="Pipeline.R"];

    D -> E -> F;
  }

  subgraph cluster_verif {
    label="Verification";
    fontsize=24;
    style=filled;
    fillcolor="#F3E5F5";
    color="#6A1B9A";
    penwidth=2;

    G [label="PSPP"];
    H [label="Rscript"];

    A -> G;
    F -> H;
  }

}
```

---

## What is the legacy-etl-compiler system?

- Takes an existing SPSS/PSPP pipeline and converts it into another language (e.g., R).
- Uses an intermediate representation to make the compiler deterministic, testable, and optimizable.
- The key intermediate representation is a **State Machine** built from the original script.

---

## Why via a State Machine?

- SPSS/PSPP scripts are *procedural* and *order-dependent*; changing the order can change results.
- A State Machine makes dependencies explicit and avoids hidden side effects.
- It lets us safely reorder, optimize, and verify transformations while preserving behavior.

---

## What is a State Machine?

- A mathematical model of a program where each operation transitions the system to a new state.
- Instead of mutating variables, we create new versions (e.g., `X1`, `X2`, `X3`).
- Each step is a deterministic state transition, which makes reasoning about pipelines easier.

---

## Order Matters (Example)

Consider the following sequential logic:

```text
X <- 1
Y <- X * 3
X <- X + 1
Z <- X * 7
output sum X, Y, Z
```

If run in this order, the values are:

- X becomes 1
- Y becomes 3
- X becomes 2
- Z becomes 14
- sum = 19

If the operations are reordered (e.g., computing Z before updating Y), the result changes.

---

## State Machine View (Same Logic)

Each step creates a new state (immutable datasets):

- `X1` is given the value 1.
- `Y` is computed from `X1` (3).
- `X2` is updated from `X1` (2).
- `Z` is computed from `X2` (14).
- Output is the sum of `X2`, `Y`, and `Z` (19).

```{dot}
digraph {
  rankdir=LR;
  node [shape=box, style=rounded, fillcolor="#E8F1FA", color="#1F4E79", fontname="Helvetica", fontsize=20];

  S0 [label="State₀\nX=1"];
  S1 [label="State₁\nY=3"];
  S2 [label="State₂\nX=2"];
  S3 [label="State₃\nZ=14"];
  S4 [label="State₄\nOutput=19"];

  S0 -> S1 -> S2 -> S3 -> S4;
}
```

---

## State Machine (Compiler-Level)

- Operations become nodes in a directed acyclic graph.
- States are the intermediate datasets produced by each node.
- The compiler can then optimize, reorder, and validate these transformations.

---

## Environment Considerations

- Linux (Rscript + PSPP easily installed)
- Windows: R via VS Code, PSPP absent -> configurable commands or skip

---

## Usage

```bash
python src/compiler.py --manifest demo/input/logic.sps \
  --pspp-cmd "echo PSPP skipped" \
  --rscript-cmd Rscript
```

---

## Questions?

*Detailed notes available in `docs/system_explanation.qmd`.*

---

## Environment Considerations

- Linux (Rscript + PSPP easily installed)
- Windows: R via VS Code, PSPP absent -> configurable commands or skip

---

## Usage

```bash
python src/compiler.py --manifest demo/input/logic.sps \
  --pspp-cmd "echo PSPP skipped" \
  --rscript-cmd Rscript
```

---

## Questions?

*Detailed notes available in `docs/system_explanation.qmd`.*
